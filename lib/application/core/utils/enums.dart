enum Flavor { DEV, UAT, QA, LIVE, SIT }
enum DeviceOS { ANDROID, HUAWEI }

enum SecurityFailureType {
  ADB,
  ROOT,
  JAILBROKEN,
  EMU,
  SECURE,
  SOURCE,
  HOOK,
  DEBUGGER,
  OBFUSCATION,
  BINDING,
}
